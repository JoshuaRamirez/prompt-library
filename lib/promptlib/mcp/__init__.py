"""MCP adapter: exposes the service as Model Context Protocol tools.

Implemented against the raw JSON-RPC wire protocol using only the standard
library, so the plugin has no install step and no dependency drift.
"""

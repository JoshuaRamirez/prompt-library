"""Search layer: the retrieval port and its lexical implementation.

`SearchStrategy` is the seam a vector-database backend plugs into later without
disturbing storage, domain, or interface layers. See docs/VECTOR-DB.md.
"""
